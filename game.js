exports.randomWord = randomWord;
// this is where the words selection comes from, it is exported from the array below

var words[Porshe, Ferrari, Lamborgini, Pagani, koenigsegg, Audi];

var randomIndex =math.floor(Math.random() * words.length);
// this randomizes what word you will get when starting a new game

var randomWord = words[randomIndex];

